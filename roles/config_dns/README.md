Config DNS
=========

Used to configure dns on devices


Role Variables
--------------

The following vars are required:

* dns_servers = list of DNS servers

```
dns_servers:
  - 8.8.8.8
  - 1.1.1.1
```